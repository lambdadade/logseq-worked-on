{
  "name": "logseq-worked-on",
  "version": "1.0",
  "description": "Adds a timestamp property to a task when it's marked as DONE",
  "targets": {
    "main": false
  },
  "default": "dist/index.html",
  "author": "lambdadade <mars@cnova.de>",
  "license": "MIT",
  "scripts": {
    "dev": "parcel ./index.html --public-url ./",
    "build": "rm -rf dist && mkdir dist && parcel build --public-url . --no-source-maps index.html"
  },
  "devDependencies": {
    "parcel": "^2.0.0",
    "typescript": "^4.8.4"
  },
  "logseq": {
    "main": "dist/index.html",
    "title": "Worked On",
    "id": "lambdadade_worked_on_ytaskd",
    "icon": "icon.png"
  },
  "dependencies": {
    "@logseq/libs": "^0.0.10",
    "dayjs": "^1.11.6"
  }
}
